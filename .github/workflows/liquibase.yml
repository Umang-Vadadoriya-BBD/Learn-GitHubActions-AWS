name: liquibase
run-name: Setting up the Liquibase database
on:
    pull_request:
        types: [closed]

jobs:
  test-output:
    runs-on: ubuntu-latest
    steps:
    - name: Test Output
      run: echo "Here is a test"
